<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/header_subject_calendar_list_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/margin_card_half"
        android:paddingBottom="@dimen/margin_card_half"
        android:paddingLeft="@dimen/margin_card"
        android:paddingRight="@dimen/margin_card"
        android:background="@color/unicap_base"
        android:textSize="22sp"
        android:textColor="@android:color/white"
        tools:text="@string/first_degree"/>

</LinearLayout>
